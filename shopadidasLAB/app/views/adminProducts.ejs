<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="/stylenavbarAdmin.css">
  <style>
    /* Add custom styles for the navigation bar and search bar */

    .search-container {
      float: right;
      margin-right: 10px;
    }

    .search-container input[type="text"] {
      padding: 6px;
      border: none;
      width: 200px;
    }

    .search-container button {
      padding: 6px 10px;
      background: #333;
      color: #fff;
      border: none;
      cursor: pointer;
    }

    /* Add custom styles for the main content */
    .main-content {
      padding: 20px;
      text-align: center;
    }

    /* Add custom styles for the login form */
    .login-form {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background-color: #f5f5f5;
    }

    .login-form input[type="text"],
    .login-form input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 3px;
    }

    .login-form button {
      width: 100%;
      padding: 10px;
      background-color: #17a5ba67;
      color: #fff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
  </style>

  <script>
    function confirmDelete() {
      return confirm("คุณต้องการลบข้อมูลProductใช่ไหม?");
    }
  </script>
</head>

<body>

  <header class="navbar">
    <%- include('../navbar/navbarAdmin.ejs') %>
  </header>

  <div class="container mt-4">
    <h3 class="text-center">แสดงProductsทั้งหมด</h3>
    <form class="form-inline justify-content-center mb-4" method="get" action="/searchingProduct">
      <div class="form-group mx-sm-3">
        <input type="text" name="q" class="form-control" placeholder="ค้นหา (ชื่อ)" value="">
      </div>
      <button type="submit" class="btn btn-primary">ตกลง</button> <a href="/admin/adminProducts/addProductForm"
        class="btn btn-success">เพิ่มProduct</a>
    </form>

    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>ProductID</th>
            <th>Image</th>
            <th>Name</th>
            <th>Detail</th>
            <th>Price</th>
            <th>Stock</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(function(item) { %>
            <tr>
              <td>
                <%= item.productsID %>
              </td>
              <td>
                <img src="<%= item.image %>" alt="<%= item.productsID%>" style="max-width: 100px;">
              </td>
             
              <td>
                <%= item.productName %>
              </td>
              <td>
                <%= item.detail %>
              </td>
              <td>
                <%= item.price %>
              </td>
              <td>
                <%= item.stock %>
              </td>
              <td><a href="/editproduct/<%= item.productsID%>">Edit</a></td>
              <td><a href="/deleteproduct/<%= item.productsID%>" onclick="return confirmDelete()">Delete</a></td>
            </tr>
            <% }); %>
        </tbody>
      </table>

    </div>
 
</body>

</html>