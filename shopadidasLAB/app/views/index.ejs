<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylenavbar.css">
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
    crossorigin="anonymous"></script>


  <style>
    /* สไตล์ของหน้าหลัก */

    /* กำหนดแบบอักษรและขอบเขตให้กับทั้งหน้าเว็บ */
    body {
      font-family: Arial, sans-serif;
      /* กำหนดแบบอักษร */
      margin: 0;
      /* ไม่มีขอบส่วนว่างที่เหลือในข้างของเว็บ */
      padding: 0;
      /* ไม่มีขอบส่วนว่างที่อยู่ข้างในของเว็บ */
      background-color: #f0f0f0;
      /* สีพื้นหลังของหน้าเว็บ */
    }

    /* สไตล์ของคอนเทนเนอร์ทั้งหมด */

    /* จัดตำแหน่งและการแสดงผลของเนื้อหาทั้งหมด */
    .content-container {
      display: flex;
      /* แสดงเนื้อหาเป็นแนวนอน */
      align-items: center;
      /* จัดกลางตามแนวตั้ง */
      justify-content: space-between;
      /* จัดกลางแนวนอนและเว้นระยะห่างเท่าๆ กัน */
      width: 80%;
      /* กำหนดความกว้างของเนื้อหา */
      max-width: 1200px;
      /* กำหนดความกว้างสูงสุดของเนื้อหา */
      margin: auto;
      /* จัดตำแหน่งกลางของหน้าเว็บ */
      margin-top: 50px;
      /* เพิ่มระยะห่างด้านบนของเนื้อหา */
      padding: 10px 0;
      /* ระยะห่างของเนื้อหาจากด้านบนและล่าง */

      border-radius: 10px;
      /* กำหนดรูปร่างมุมของกล่องเนื้อหา */

    }

    /* สไตล์ของส่วนซ้าย */

    /* กำหนดขนาดของส่วนซ้ายและการจัดตำแหน่งของเนื้อหา */
    .left-content {
      flex: 1;
      /* เพื่อให้ส่วนซ้ายขยายเต็มพื้นที่ที่ว่างอยู่ */
      padding: 10px;
      /* ระยะห่างของเนื้อหาภายในส่วนซ้าย */
      text-align: center;
      /* จัดตำแหน่งข้อความซ้าย */
    }



    /* สไตล์ของข้อมูลร้านค้า */

    /* กำหนดขนาดของกล่องข้อมูลร้านค้าและการจัดตำแหน่งของเนื้อหา */
    .store-details {
      margin: 50px auto;
      /* ระยะห่างของกล่องข้อมูลร้านค้าจากด้านบนและล่าง */
      max-width: 800px;
      /* กำหนดความกว้างสูงสุดของกล่องข้อมูลร้านค้า */
      padding: 20px;
      /* ระยะห่างของเนื้อหาภายในกล่องข้อมูลร้านค้า */
      background-color: #ffffff;
      /* สีพื้นหลังของกล่องข้อมูลร้านค้า */
      border-radius: 10px;
      /* กำหนดรูปร่างมุมของกล่องข้อมูลร้านค้า */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      /* เงาของกล่องข้อมูลร้านค้า */
    }

    .content {
      flex: 1;
    }
  </style>

</head>

<body>

  <header class="navbar">
    <%- include('../navbar/navbar.ejs') %>
  </header>

  <!-- ส่วนของหน้าแรก(ตรงกลางถัดจาก HOME) NN -->
  <div class="container col-xxl-8 px-4 py-5">
    <div class="row flex-lg-row-reverse align-items-center g-5 py-5">
      <div class="col-10 col-sm-8 col-lg-6">
        <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/2/20/Adidas_Logo.svg/1200px-Adidas_Logo.svg.png"
          class="d-block mx-lg-auto img-fluid" alt="Bootstrap Themes" width="700" height="500" loading="lazy">
      </div>
      <div class="col-lg-6">
        <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">IDKB SHOP</h1>
        <p class="lead">ยินดีต้อนรับเข้าสู่ร้าน IDK SHOP เว็บนี้ทำเพื่อการศึกษาเท่านั้น
        </p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
          <a href="/Product" type="button" class="btn btn-primary btn-lg px-4 me-md-2">ดูสินค้า</a>
          <a href="/contactUS" type="button" class="btn btn-outline-secondary btn-lg px-4">ติดต่อเรา</a>
        </div>
      </div>
    </div>
  </div>
  <!-- END NN -->

  <script> src = "js/bootstrap.min.js"</script>
</body>

</html>